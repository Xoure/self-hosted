version: '3.8'

services:
#  reverse:
#    image: cloudflare/cloudflared:latest
#    command: tunnel --no-autoupdate run --token eyJhIjoiZjAyYzI3YTY5MDllYzdmMTJiMzQ1NGZhYjIzM2I3MWUiLCJ0IjoiNzUzYTRmZTEtN2VlMS00M2QzLTlkYzUtOTUyYWEwZmYwYWY5IiwicyI6Ik1tUTBOMkpqWmpZdE9XUmlPUzAwT1dVMkxUZzJaR0l0WXpFMll6QmhPVFU1TldJeiJ9
#    restart: always
#    networks:
#      - host

  database:
    image: mongo
    restart: always
    volumes:
      - /share/Container/revolt/revolt_data/data/db:/data/db
    network_mode: host

  redis:
    image: eqalpha/keydb
    restart: always
    network_mode: host

  rabbit:
    image: rabbitmq:4
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: rabbituser
      RABBITMQ_DEFAULT_PASS: rabbitpass
    volumes:
      - /share/Container/revolt/revolt_data/data/rabbit:/var/lib/rabbitmq
    network_mode: host

  minio:
    image: minio/minio
    command: server /data
    volumes:
      - /share/Container/revolt/revolt_data/data/minio:/data
    environment:
      MINIO_ROOT_USER: minioautumn
      MINIO_ROOT_PASSWORD: minioautumn
      MINIO_DOMAIN: minio
    network_mode: host
    extra_hosts:
      - revolt-uploads.minio:127.0.0.1
      - attachments.minio:127.0.0.1
      - avatars.minio:127.0.0.1
      - backgrounds.minio:127.0.0.1
      - icons.minio:127.0.0.1
      - banners.minio:127.0.0.1
      - emojis.minio:127.0.0.1
    restart: always

  api:
    image: ghcr.io/revoltchat/server:20250210-1
    depends_on:
      database:
        condition: service_started
      redis:
        condition: service_started
      rabbit:
        condition: service_started
    volumes:
      - /share/Container/revolt/Revolt.toml:/Revolt.toml
    restart: always
    network_mode: host

  events:
    image: ghcr.io/revoltchat/bonfire:20250210-1
    depends_on:
      database:
        condition: service_started
      redis:
        condition: service_started
    volumes:
      - /share/Container/revolt/Revolt.toml:/Revolt.toml
    restart: always
    network_mode: host

  web:
    image: ghcr.io/revoltchat/client:master
    environment:
      HOSTNAME: revolt.app-xoure.com
      REVOLT_PUBLIC_URL: https://api.app-xoure.com
    restart: always
    network_mode: host

  autumn:
    image: ghcr.io/revoltchat/autumn:20250210-1
    depends_on:
      database:
        condition: service_started
      createbuckets:
        condition: service_started
    volumes:
      - /share/Container/revolt/Revolt.toml:/Revolt.toml
    restart: always
    network_mode: host

  january:
    image: ghcr.io/revoltchat/january:20250210-1
    volumes:
      - /share/Container/revolt/Revolt.toml:/Revolt.toml
    restart: always
    network_mode: host

  crond:
    image: ghcr.io/revoltchat/crond:20250210-1-debug
    depends_on:
      database:
        condition: service_started
      minio:
        condition: service_started
    volumes:
      - /share/Container/revolt/Revolt.toml:/Revolt.toml
    restart: always
    network_mode: host

  pushd:
    image: ghcr.io/revoltchat/pushd:20250210-1
    depends_on:
      database:
        condition: service_started
      redis:
        condition: service_started
      rabbit:
        condition: service_started
    volumes:
      - /share/Container/revolt/Revolt.toml:/Revolt.toml
    restart: always
    network_mode: host

  createbuckets:
    image: minio/mc
    depends_on:
      minio:
        condition: service_started
    entrypoint: >
      /bin/sh -c "
      while ! /usr/bin/mc alias set minio http://minio:9000 minioautumn minioautumn; do
        echo 'Waiting for MinIO...' && sleep 1;
      done;
      /usr/bin/mc mb minio/revolt-uploads || true;
      exit 0;
      "
    network_mode: host

networks:
  host:
    name: host
    external: true
